{
  "name": "islamic-online-learning",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "db:push": "cd server && npx prisma db push",
    "db:seed": "cd server && npx tsx prisma/seed.ts",
    "build": "npm install --include=dev --prefix client && npm run build --prefix client && npm install --include=dev --prefix server && npm run build --prefix server",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
